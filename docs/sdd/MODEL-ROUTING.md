# Model routing

The harness ships with the R17.4.5 qualified champion as the conservative default. Model routing is an operational policy, not project specification. Override only after matched benchmark evidence. `AGENT_HARNESS_DEFAULT_MODEL` may select a model already present in `.agents/model-routing.json`; expand the catalog before using an unlisted model.
