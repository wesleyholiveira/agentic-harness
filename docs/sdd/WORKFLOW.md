# Specification-Driven Development

`Product Discovery → impact-selected reviews → Technical Refinement → dynamic implementation DAG → QA → conditional Operational Readiness → Product Acceptance`.

The first three stages establish intent and executable authority. Technical Refinement owns the implementation plan; the Runtime compiler derives task dependencies dynamically. Specialist manifests are capability/ownership catalogs, not a static workflow.
