# Context Packet
Machine authority: `.agents/schemas/context-packet.schema.json`.
Include only the smallest authoritative context needed for the task; reference large artifacts lazily by content hash.
