# <Change> — Implementation Plan
Use `.agents/schemas/implementation-plan.schema.json` as machine authority.
## Work items
For each: owner, exact paths, dependencies, acceptance criteria, executable validation, migration/contract flags.
