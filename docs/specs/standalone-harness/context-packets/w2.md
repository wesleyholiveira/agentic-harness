# Context Packet W2 — Dual-root runtime

## Authority

- `docs/specs/standalone-harness/PRD.md`;
- `docs/specs/standalone-harness/DESIGN.md`;
- applicable ADRs under `docs/adr/`;
- schemas under `.agents/schemas/`.

## Scope

Separate harness authority root from consuming-project workspace/evidence root and preserve that distinction under inherited host environment variables.

## Inputs

- `.agents/runtime/**`
- `apps/context-engine/**`
- `bin/harness.mjs`
- `scripts/harness-bootstrap.mjs`
- `scripts/harness-doctor.mjs`
- `scripts/internal/**`

## Constraints

Preserve Runtime durable-authority, fencing/idempotency, SDD acceptance and dual-root invariants. Treat product/domain artifacts as consuming-project authority, not harness source. A host-level `AGENT_HARNESS_PROJECT_ROOT` that resolves to harness source, including a different outer checkout, must not capture a nested external consumer invocation.

## Evidence expected

Source diff, focused tests/static checks, and a schema-valid handoff.
